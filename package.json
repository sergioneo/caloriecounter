{
  "name": "caloriecounter-pwa",
  "version": "1.0.0",
  "description": "AI-powered calorie tracking PWA with Claude integration",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "npm run build:client",
    "build:client": "cd client && vite build",
    "preview": "cd client && vite preview"
  },
  "workspaces": [
    "client",
    "shared"
  ],
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "firebase": "^10.7.1",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.4.1",
    "@types/node": "^20.10.5",
    "concurrently": "^8.2.2",
    "netlify-cli": "^17.10.1",
    "typescript": "^5.3.3"
  }
}
